{% extends 'base.html.twig' %}

{% block title %}{% if edit %}{{ 'admin.user.title.edit'|trans }}{% else %}{{ 'admin.user.title.new'|trans }}{% endif %}{% endblock %}

{% block body %}
    <h1>{{ block('title') }}</h1>

    {{ include('admin_user/_form.html.twig') }}

    <a href="{{ path('app_admin_user_index') }}">back to list</a>
{% endblock %}
